# Workspace

Workspace watches Teaching and Interactive Runs owned by the local `web` or `mcp` process, and hands the browser to the user on request during a Run. Teaching is user-led throughout. The user drives the browser and the agent only observes. Teaching setup remains outside the recording until the user presses Start, and Stop ends capture without closing the browser. With no activity, either command reports that no sessions are active. With a live Interactive Run, Workspace shows the browser and one control button, both under the same floating dock a Teaching session gets. During Takeover, the user drives that browser with the toolbar and the canvas. A live Teaching session has no control exchange. The canvas and toolbar are already the user's.

## Sub-features

- `workspace-nav` opens the sole Workspace route at `/`; Create, Audit, and Agent mode links are absent. The header band appears on a Run Summary and an unknown route, and steps aside for every Workspace that owns the viewport.
- `workspace-chrome` shows one floating `Workspace dock` over a full-bleed browser, with the wordmark, the session select, the state badge, the next-step sentence, and at most one primary action, at desktop width and at 390px. Teaching, a Dry Run, and an Interactive Run all render it, and none of them shows a session status sidebar.
- `workspace-run-summary` opens a finished Interactive Run at `/?run=<id>` with persisted assessments and video.
- `agent-empty-web` shows `No browser session` on a `web` launch with no Teaching or Run, and opens a Teaching session from the canvas without MCP.
- `agent-empty-mcp` shows `No browser session` on an MCP launch with no Teaching or Run.
- `agent-bad-session` shows a notice when a `?session=` id is missing and no live session exists. With a live session, Workspace shows it and replaces the missing id in the URL.
- `agent-live-session` shows a live session's browser and the dock that carries its state.
- `agent-same-document-snapshot` returns destination nodes with the destination URL after an agent action routes without loading a new document.
- `agent-snapshot-context` reports scripted location rows as named clickable controls, attaches product context to repeated buttons, and omits decorative images.
- `agent-watching-readonly` disables the browser toolbar and marks the canvas read-only while the agent holds control.
- `agent-takeover-controls` enables history, address, and canvas input once the user takes control.
- `agent-takeover-timeline` records what the user did as `"actor":"user"` in the session's timeline, and follows the browser's URL. The timeline is read over MCP; Workspace no longer renders it.
- `agent-return-control` hands the browser back, and the toolbar goes quiet again.
- `agent-run-dock` shows a live Interactive Run's Flow Skill, active Agent Step number, ceiling extensions, and single control button in the dock, the Step's name and `Done when:` line behind the coverage button, and a live Dry Run's rehearsed Flow Skill and changed inputs.
- `agent-teaching-inspect` outlines the live element under the pointer during `recording`, attaches a comment as a Teaching instruction, and counts it in the dock.
- `agent-teaching-dock-actions` renames the Flow Skill in `setup`, and copies the agent prompt or deletes the recording in `ready`.
- `agent-teaching-recording-boundary` keeps setup out of Teaching artifacts, starts every capture source from the Workspace, and stops them without closing the browser setup.
- `workspace-input-order` sends rapid clicks and typing through the live Workspace canvas and checks their result in the nested Page.
- `agent-teaching-flow-skill-learning` discovers the stopped Teaching Recording from another MCP process, reads the authoring skills Contingency serves as MCP resources, claims the recording once, pages through its semantic timeline, fetches keyframes one at a time, refuses a package that breaks the Flow Skill contract, and atomically saves a parameterized one.
- `agent-teaching-flow-skill-dry-run` runs the saved Flow Skill in a fresh browser context with changed inputs, keeps evidence on failure, pass, or rejection, and deletes the temporary recording only after **Verify flow**. Cleanup resumes after restart.
- `agent-execution-boundary` pauses refused domains, new objectives, and per-attempt confirmation, relays each pause as a Pending Decision the agent resolves over MCP, and preserves priority Takeover.
- `agent-pending-decisions` relays Execution Boundary and runtime Variable allow, supply, or refuse through MCP while Workspace mirrors pending and resolved status without action buttons.
- `agent-runtime-variables` creates one Pending Decision for each missing runtime Variable, accepts supply or refusal through MCP, and keeps Workspace read-only.
- `agent-private-variables` enters a reusable account and password plus a runtime OTP without putting their literals in the Teaching Timeline or the saved Flow Skill.
- `workspace-browser-setup` configures the MCP-owned browser during teaching setup: identity, viewport, Emulation environment, and the storage and network panels for the tab the session is showing.

## How to get to it (user POV)

- Choose the `Workspace` link in primary navigation.
- Open `/` directly.
- Open the URL printed by `contingency mcp`: `http://127.0.0.1:<port>/`.
- Follow an MCP client's local Workspace link that includes `?session=<id>`.

## Driving it with control-contingency

Preconditions:

- For `workspace-nav` and `agent-empty-web`, the verification instance is a `web` launch (the default `control-contingency launch`).
- For `agent-empty-mcp`, run `control-contingency mcp start` on the same verify directory after `launch` and `doctor`. That binds MCP on an ephemeral port with the isolated `stateDir`. Do not attach to an MCP process you did not start.
- For every live-session sub-feature, `mcp start` and `ecommerce start` must both be running. Create the session with `control-contingency mcp call`, which speaks MCP to that same process — a session exists only inside the process that owns it, so nothing else can conjure one.

- **Nav entry.** The header band belongs to the Views that still have one: a Run Summary and an unknown route. Open a Run Summary or an unknown route with `control-contingency browser goto --path /nowhere`, then `control-contingency browser click --role link --name Workspace`. The `Workspace` link is current on `/`. Workspace and the Contingency wordmark are home links: both clear the selected session or Run Summary and return to the session workspace.
- **Empty on web.** After the query settles, run `control-contingency browser wait --role heading --name "No browser session" --timeout-ms 15000`. There is no header band here or on any live session: the dock carries the wordmark, and the ARIA snapshot has no `link` named `Workspace`.
- **Direct route.** Open `/` without using nav. Run `control-contingency browser goto --path /`, then wait for the same empty heading.
- **Open a session from the canvas.** `contingency web` owns Teaching, so the empty canvas starts one without MCP. Run `control-contingency browser click --role button --name "Open browser session"`, then `control-contingency browser wait --role button --name "Start recording" --timeout-ms 20000`. The dock badge becomes `Not recording`. Exactly one control is named `Open browser session`: search the empty-state ARIA artifact and require one match.
- **Unknown session query with no sessions.** Run `control-contingency browser goto --path "/?session=not-a-session"`, then `control-contingency browser wait --role alert --has-text "That session id did not resolve"`. The empty Workspace still shows `No browser session` and `Open browser session`.
- **Unknown session query with a live session.** Open a live session first, then run `control-contingency browser goto --path "/?session=not-a-session"`. Wait for the `Agent Session` combobox to show the live session. Run `control-contingency browser url` and require `?session=<shown id>`. The missing-session alert is absent. Reload to confirm the same session opens.
- **Empty on MCP.** Start MCP on the verify instance. Run `control-contingency mcp start`, then `control-contingency browser goto --url "$mcpUrl"` using the printed `mcpUrl`. Wait with `control-contingency browser wait --role heading --name "No browser session" --timeout-ms 15000`.
- **Proof (web empty).** Capture the empty web state. Run `control-contingency browser goto --path /`, wait for the empty heading, then `control-contingency browser snapshot --aria --path workspace/empty-web.aria.txt` and `control-contingency browser screenshot --path workspace/empty-web.png`. Both show the `Workspace dock` with `Contingency`, the badge `No session`, and `No browser session` on the canvas.
- **Proof (390px).** Run `control-contingency browser resize --width 390 --height 844`, then `control-contingency browser screenshot --path workspace/empty-web-390.png` and `control-contingency browser snapshot --aria --path workspace/empty-web-390.aria.txt`. `Open browser session` is still on screen and the next-step sentence is hidden rather than ellipsized. Return to desktop with `control-contingency browser resize --width 1440 --height 900`.
- **Proof (MCP empty).** After `mcp start`, capture the empty MCP state. Run `control-contingency browser goto --url "$mcpUrl"`, wait for the empty heading, then `control-contingency browser snapshot --aria --path workspace/empty-mcp.aria.txt` and `control-contingency browser screenshot --path workspace/empty-mcp.png`.

### Live session and Takeover

- **Start a session.** Give the agent a browser on the local shop. Run `control-contingency mcp call --tool agent_session_start --params "{\"clientName\":\"verify\",\"clientVersion\":\"1.0\",\"operationId\":\"verify-agent-1\",\"url\":\"$ECOMMERCE_URL\",\"viewport\":{\"deviceScaleFactor\":1,\"height\":720,\"width\":1024}}"`. Stdout is the session snapshot; export its `id` as `sessionId` and its `viewUrl`.
- **Open the live view.** Run `control-contingency browser goto --url "$viewUrl"`, then `control-contingency browser wait --role button --name "Take control" --timeout-ms 20000`. There is one `Workspace dock` region, no `Workspace` heading, and no session status sidebar. The dock badge reads `Live` once frames arrive, and `Take control` is the dock's only control button.
- **Route within the document.** Call `agent_browser_snapshot` and note the ref for the `Choose delivery area` button. Call `agent_browser_act` with that ref and a fresh operation id. Save stdout as `workspace/same-document-act.json`: `snapshot.url` ends in `/shop.html/delivery`, its nodes include heading `Delivery area` and button `Use current location`, and no node is named `Choose delivery area`.
- **Proof (same-document reread).** Call `agent_browser_snapshot` again and save stdout as `workspace/same-document-reread.json`; it reports the same URL and destination controls. In Workspace, save `workspace/same-document.aria.txt` and `workspace/same-document.png`; the live canvas is at the destination. Read the completed agent click from `agent_session_get`'s timeline, not from the View.
- **Start the snapshot fixture.** Start a separate Run session with `agent_session_start`, using `${ECOMMERCE_URL%/*}/snapshot-context.html`, a fresh operation id, and the same viewport and client fields as the shop session above. Save its `sessionId` and `viewUrl`. The page shows two delivery results and two products with buttons both named `Add to cart`.
- **Prove the Browser Snapshot.** Call `agent_browser_snapshot` for that session and save stdout as `workspace/snapshot-context-before.json`. Require exactly one `clickable:true` node named `Sector 14 Gurugram` and one named `Sector 144 Noida`. The two `Add to cart` buttons have different `context` values naming `Backpack` and `Bike Light`. No unnamed `image` or `presentation` node appears, and the named `Shop logo` image remains.
- **Act on the result row.** Call `agent_browser_act` with `{"type":"click","ref":"<Sector 144 Noida ref>"}` and a fresh operation id. Save stdout as `workspace/snapshot-context-act.json`. Call `agent_browser_snapshot` again and save stdout as `workspace/snapshot-context-after.json`; its paragraph reads `Chosen: Sector 144, Noida`. Open the session's `viewUrl` in the verification browser, then save `workspace/snapshot-context.aria.txt` and `workspace/snapshot-context.png` to show the matching Workspace page.
- **Watching is read-only.** Before taking control, snapshot the toolbar. Run `control-contingency browser snapshot --aria --path workspace/takeover-watching.aria.txt`. `Go back`, `Go forward`, `Reload page`, and `Browser address` are all `[disabled]`, the address placeholder reads `Take control to drive the browser`, and the canvas `Live browser viewport` is `aria-readonly="true"`.
- **Take control.** Run `control-contingency browser click --role button --name "Take control"`, then `control-contingency browser wait --role button --name "Return control" --timeout-ms 15000`. The same toolbar handles are now enabled and the canvas is `aria-readonly="false"`.
- **Navigate by address.** Run `control-contingency browser fill --role textbox --name "Browser address" --value "${ECOMMERCE_URL%/*}/catalog.html"`, then `control-contingency browser press --key Enter --role textbox --name "Browser address"`. A bare host is completed to `https://`.
- **Navigate by history.** Run `control-contingency browser click --role button --name "Go back"`, then `control-contingency browser click --role button --name "Reload page"`. Each is dispatched one at a time; a second click while one is pending is refused by the disabled state rather than racing it.
- **Scroll the page.** Wheel events belong to the nested browser, not to Contingency chrome. Drive them with `computerUse` at the verification URL over the `Live browser viewport` canvas. The page under the canvas scrolls; the View behind it does not.
- **Proof (what the user did).** Read the session back with `control-contingency mcp call --tool agent_session_get --params "{\"sessionId\":\"$sessionId\"}"`. `currentUrl` is where the browser now is, and the timeline carries `The user took control`, `Navigate to <url>`, `Go back`, and `Reload the page`, each with `"actor":"user"` and `"outcome":"completed"`. Workspace itself shows only the dock: assert the timeline over MCP.
- **Proof (the view itself).** Run `control-contingency browser snapshot --aria --path workspace/takeover-driving.aria.txt` and `control-contingency browser screenshot --path workspace/takeover-driving.png`.
- **Return control.** Run `control-contingency browser click --role button --name "Return control"`, then `control-contingency browser wait --role button --name "Take control" --timeout-ms 15000`. The toolbar is disabled again, and `agent_session_get`'s timeline gains `The user returned control to the agent`.

### The Run dock

- **Read the Run in the dock.** With an Interactive Run open at its `viewUrl`, run `control-contingency browser snapshot --aria --path workspace/run-dock.aria.txt`. The `Workspace dock` names the Flow Skill and reads `Agent Step <n> of <total>.` The row itself carries no Step name and no `Done when:` line: they would break the dock's single row, so they sit behind the coverage button, whose accessible name is `<executed> of <total> Agent Steps executed. Agent Step <n> of <total>.` (#238). The `Agent Session` option reads `<flow> · Interactive Run` and carries no session id.
- **Open the Agent Step detail.** Click that coverage button, then snapshot again. The popover is titled `Agent Step <n> of <total>` and holds the Step's name and its `Done when:` line in full. Close it and confirm the dock is back to one row.
- **Raise a ceiling.** Run `control-contingency browser click --role button --name "Extend Agent Step ceiling"`, then read the session back: `run.ceilings.extensions` has grown. There is no MCP tool for this; the dock button is the only way either ceiling grows. Both buttons are absent once the Run has ended.
- **Read a Dry Run in the dock.** Open a live Dry Run's `viewUrl`. The dock reads `Rehearsing the flow skill <name>.` and names the inputs that differ from the recording, and the `Agent Session` option reads `<flow> · Dry Run`. Save `workspace/dry-run-dock.aria.txt`.
- **Proof (390px).** Run `control-contingency browser resize --width 390 --height 844` over a live Run and save `workspace/run-dock-390.png`. The control button is still on screen and the next-step sentence is hidden rather than ellipsized. Return to desktop afterwards.

### Teaching and Flow Skill learning

`mcp start` points the Catalog Root at `$CONTINGENCY_VERIFY_DIR/state/catalog`, so saved Flow Skills and Teaching Recordings never land in the repository's `.contingency`.

- **Select the Catalog Root.** Run `control-contingency mcp call --tool agent_catalog_select --params "{\"operationId\":\"verify-select-1\",\"root\":\"$CONTINGENCY_VERIFY_DIR/state/catalog\"}"`. Stdout reports that exact root. Repeating the same call returns the same result.
- **Start a Teaching session.** Same as above with `"activity":"teaching"` and `"name":"add-anvil"` in the params. The snapshot carries `flowSkillName`, `recordingId`, `captureState._tag:"setup"`, and zero Teaching progress. The `Agent Session` selector shows `add-anvil` alone; the dock badge carries the state, and no raw session id appears in either. A Run session carries null Teaching fields.
- **Prove setup is outside capture.** Open the Teaching `viewUrl`, wait for the button `Start recording`, and save `workspace/teaching-setup.aria.txt` and `workspace/teaching-setup.png`. The recording region says `Not recording` and explains that setup is not captured. Navigate or sign in before Start. The recording directory contains only `manifest.json`; it has no Trace, video, events, or keyframes.
- **Start capture.** Run `control-contingency browser click --role button --name "Start recording"`, then wait for `Stop recording`. Save `workspace/teaching-recording.aria.txt` and `workspace/teaching-recording.png`. Reread with `agent_session_get`; `captureState._tag` is `recording`, `startedAt` is present, and the elapsed timer is based on it.
- **Relay an instruction.** After Start, run `control-contingency mcp call --tool agent_teaching_instruction_record --params "{\"sessionId\":\"$sessionId\",\"operationId\":\"verify-instr-1\",\"text\":\"Add the first product to the cart.\"}"`. The timeline gains `The user gave an instruction` and `instructionCount` becomes 1. Before Start, this call is refused.
- **Prove the agent cannot act.** Teaching is user-led, so call `agent_browser_act` once with any action and a fresh operation id. It exits `2` with `agent_control_unavailable`, and the message says Teaching is user-led and points at `agent_teaching_instruction_record`. `agent_session_takeover_request` is refused the same way, and the View offers no `Take control` or `Return control` button — only `You are demonstrating this journey`.
- **Demonstrate.** The canvas is live during setup and recording because the user already holds the browser. After Start, drive the fixture through the Workspace with `computerUse` over the streamed viewport (add `ANVIL-001` by SKU, add it to the cart, view the cart), or take the list route instead (click `Add Giant anvil to cart` under `Featured products`, then `View cart`), and use the address bar for navigation. `agent_browser_snapshot` still reads the Page at any point. Every recorded action, failed ones included, is captured as `actor: "user"` with the Snapshot before and after; consecutive text edits coalesce into one semantic `fill`.
- **Check rapid input.** Start a fresh Teaching session at `${ECOMMERCE_URL%/*}/input-check.html` with a 1024 by 720 viewport. Open its `viewUrl`, wait for `Start recording`, then click it. Run `control-contingency browser input-check --session-id "$sessionId"`. The command clicks the fixture button twice and types `rapid typing 123` into the field through the live Workspace canvas. It reads `agent_browser_snapshot` until the Page reports `Clicks: 2` and the full text, or exits nonzero after five seconds. Read `agent_session_get` for completed user input entries, then save `workspace/input-order.aria.txt` and `workspace/input-order.png`. Read `agent_browser_snapshot` again as the second observation; it must still show the same count and field value. Stop recording and clean up the isolated session.
- **Add from the featured list.** The shop fixture offers a second add route that skips the SKU textbox. Over `computerUse`, click the button `Add Giant anvil to cart` in the `Featured products` section. The paragraph `0 items in cart` becomes `1 item in cart`, exactly as the `Add SKU to cart` button leaves it. Click `View cart`; the cart page lists `SKU ANVIL-001`. Each featured product carries its own button named after it (`Add Workbench to cart`, `Add Safety goggles to cart`), so a recipe can target one product by name.
- **Stop capture.** Run `control-contingency browser click --role button --name "Stop recording"`. The dock first says `Saving`, then `Recording saved`, while the live browser remains available. Reread the session and manifest. Require non-empty `events.jsonl`, `recording.webm`, and `trace.zip` in `$CONTINGENCY_VERIFY_DIR/state/catalog/.recordings/$recordingId/`. The first JSON line is `started`, the last is `stopped`, and no setup URL or setup action appears between them.
- **Rename before capture.** From `Not recording`, run `control-contingency browser click --role button --name "Rename flow"`, `control-contingency browser fill --role textbox --name "Flow skill name" --value "add-anvil-v2"`, then `control-contingency browser click --role button --name "Save name"`. The selector reads `add-anvil-v2`. After Start, `Rename flow` is absent: the bundle on disk is already filed under the name it began with.
- **Comment on an element.** While recording, run `control-contingency browser click --role button --name "Inspect an element and comment"`. Move the pointer over the streamed viewport with `computerUse`; the live element under it is outlined from a Browser Snapshot, not from the canvas bitmap. Click to freeze it, fill `Describe the change`, and click `Attach`. The dock counts `1 comment`, the timeline gains `The user gave an instruction`, and `instructionCount` becomes 1. `Escape` leaves inspect mode. There is no second instruction field anywhere in the View.
- **Proof (inspect comment).** Save `workspace/teaching-inspect.aria.txt` and `workspace/teaching-inspect.png` with the comment attached and the count visible.
- **Copy the agent prompt.** From `Recording saved`, run `control-contingency browser click --role button --name "Copy agent prompt"`. The clipboard holds a prompt naming the recording and the Flow Skill. `Learn flow`, `Dry run`, `Verify flow`, `Reject flow`, and `Retry cleanup` are absent rather than disabled: search the ARIA artifact for each and require zero matches.
- **Confirm the copy on the button.** Immediately after that click, save the ARIA artifact. The button reads `Copied agent prompt`, and the dock's `Copy confirmation` region carries the same sentence, so the confirmation is words rather than a colour. Save it again more than two seconds later: the button is back to `Copy agent prompt` and the region is empty. Every other `Copy` button in the dock confirms the same way, each naming what it copied. A refused write shows the dock's error and no `Copied` label at all (#214).

- **Audit the dock for inert buttons.** In every state you reach, save the ARIA artifact and require zero **buttons** named exactly `Dry run`, `Learn again`, `Read failure`, `Read flow skill`, or `Run flow`. Match on the accessible name of a `button` node alone: the status badge still reads `Dry run`, `Dry run failed`, or `Dry run passed`, and the dock's polite region repeats it, so a whole-artifact search fails a correct dock. A button whose label names an action it cannot perform is the defect (#210). Every dock button either mutates the recording or begins with `Copy`, and a `Copy` button fills the clipboard and says so.
- **Discover from another process.** Save `recordingId`, stop the isolated MCP process, and start it again. `agent_sessions_get` is empty because browser sessions belong to their creating process. Call `control-contingency mcp call --tool agent_teaching_recordings_list`. The stopped recording is present with its Flow Skill name and `lifecycle:"ready"`; the result contains no Catalog Root or artifact path.
- **Wait across processes.** Before Stop in a separate drive, call `control-contingency mcp call --tool agent_teaching_recordings_list --params "{\"recordingId\":\"$recordingId\",\"timeoutMs\":60000}"` from the MCP process. Naming one recording is the waiting form of the list: it polls the manifest written by `web` or the recording process and answers with that single recording at `recording`, `ready`, or `failed`. A shorter elapsed timeout exits `2` with `teaching_recording_timeout`.
- **Claim once.** Run `control-contingency mcp call --tool agent_teaching_recording_claim --params "{\"action\":\"take\",\"recordingId\":\"$recordingId\",\"operationId\":\"verify-learning-claim\"}"`. The result carries the `claim` beside the recording's new state. Repeating that operation id returns the same claim. A second process with a different operation id exits `2` with `teaching_recording_conflict` while the first owner lives.
- **Read the semantic timeline.** Run `control-contingency mcp call --tool agent_teaching_timeline_get --params "{\"recordingId\":\"$recordingId\",\"claimOperationId\":\"verify-learning-claim\"}"`. Actions carry `before`, `after`, `appeared`, and `disappeared`; Instructions and URL changes retain their order. Keyframes carry only `id` and `hash`. Follow `nextCursor` until it is null. Every page reports `maxCharacters:65536`; a single event too large for a page exits `2` with `teaching_timeline_too_large`.
- **Fetch one keyframe.** Take one keyframe id from the timeline and call `control-contingency mcp call --tool agent_teaching_keyframe_get --params "{\"recordingId\":\"$recordingId\",\"claimOperationId\":\"verify-learning-claim\",\"keyframeId\":\"<keyframe id>\"}"`. The result contains one base64 PNG and its hash. An unknown id exits `2` with `teaching_recording_not_found`.
- **Read Contingency's authoring skills.** Before writing anything, run `control-contingency mcp resources`. The list carries `contingency://skill/writing-for-agents`, `contingency://skill/writing-for-agents/SKILL-MECHANICS.md`, `contingency://skill/technical-writing`, and `contingency://skill/unslop`, each `text/markdown`; search it for `skill-creator` and require zero matches. Read one with `control-contingency mcp resource --uri "contingency://skill/unslop"`; stdout is the vendored markdown and its frontmatter names the skill. These come from the CLI install, so they answer the same way when the host has no skills of its own.
- **Refuse an unsafe path.** Put a previous `SKILL.md` under `$CONTINGENCY_VERIFY_DIR/state/catalog/<flow-name>/`, then call `agent_flow_skill_save` with a file whose path is `../outside.md` or whose content is empty. The call exits `2` with `teaching_recording_invalid`; rereading the previous file proves it was not replaced.
- **Refuse an uncontracted package.** Save a `SKILL.md` that has no frontmatter, or whose frontmatter `name` is not the Flow Skill name, or that names an element reference such as `e12`, or whose numbered step has no `Done when:` line. The call exits `2` with `teaching_recording_invalid` and carries one `diagnostics` entry per broken property, each `path` starting at the file. The previous `SKILL.md` is unchanged and the recording is still `learning`, not `skill-drafted`.
- **Save the proposed Flow Skill.** Call `agent_flow_skill_save` with a package that meets the contract: `SKILL.md` opens with frontmatter whose `name` is the Flow Skill name, carries a `description` and an `inputs` list, uses `{{city}}` and `{{delivery_area}}` in its steps, ends every numbered step with `Done when: <observable outcome>`, and links `references/accessibility.md`. That reference lists each target as `- role=<role> name="<name>" context="<context>"` and carries a `## Why these targets are stable` heading. Reread both files under the isolated Catalog Root. The saved `SKILL.md` now carries a `hosts` sequence naming `127.0.0.1` and an `emulation` block whose `viewport` matches the recording manifest, stamped by Contingency: write a different `hosts` value in the submitted package and require the saved file to hold the demonstrated one instead. Repeating the same save returns the original result. The recording manifest is `skill-drafted` only after the package is in place.
- **Prove the Flow Skill is parameterized.** Demonstrate the delivery journey on the shop fixture before Stop: click `Choose delivery area`, then `Select manually`, fill the textbox `City` and the textbox `Search for your delivery area`, and click `Confirm delivery area`. The status reads `Delivering to <area>, <city>`. Read the saved `SKILL.md` afterwards: it names `{{city}}` and `{{delivery_area}}`, not the demonstrated pair, so a later Dry Run can change either without the recording. Search it for element references, `trace.zip`, and `recording.webm` and require zero matches.
- **Hand the Dry Run over.** From `Flow skill drafted`, run `control-contingency browser click --role button --name "Copy dry run prompt"`. The clipboard holds a prompt naming the Flow Skill, the recording id, `agent_flow_skill_dry_run_start`, and `agent_flow_skill_dry_run_report`, and it tells the agent to change at least one input. Starting the Dry Run is the agent's work: the dock cannot collect changed inputs, so it copies the prompt rather than offering a button that starts nothing.
- **Start a fresh Dry Run.** Call `agent_flow_skill_dry_run_start` with the recording id, the fixture URL, and the required inputs again. Change the demonstrated city, the delivery area, or both, and set `changed:true` on each changed value. The result returns the saved Flow Skill files and a new Run session whose Emulation matches the manifest. Its session id differs from the Teaching session id. Open its `viewUrl` and drive the returned `SKILL.md` with the browser tools.
- **Keep evidence after failure.** Report a failed observable outcome with `agent_flow_skill_dry_run_report`. The manifest becomes `dry-run-failed`, the dock offers **Copy dry run prompt**, **Copy failure**, and **Copy learn again prompt**, and every file under `.recordings/$recordingId/` remains. Start another Dry Run with a fresh operation id.
- **Fix the package on the original claim.** From `dry-run-failed`, call `agent_flow_skill_save` again with the claim operation id from the first claim and a fresh save operation id. It succeeds without another `agent_teaching_recording_claim`, and the manifest returns to `skill-drafted`. A save against a recording with no claim is refused with `teaching_recording_unclaimed` and a message naming `agent_teaching_recording_claim`; a save after a passing Dry Run is refused with `teaching_recording_conflict` naming `agent_flow_skill_decide`.
- **Keep evidence after pass.** Reach `Delivering to <changed area>, <changed city>`, then report `outcome:"passed"`. The manifest becomes `dry-run-passed`; `references/verification.md` records the time, changed non-secret inputs, and the observable outcome. `recording.webm`, `trace.zip`, `events.jsonl`, keyframes, and screenshots still exist. The dock offers **Verify flow**, **Reject flow**, and **Copy flow skill path**.
- **Reject without cleanup.** Call `agent_flow_skill_decide` with `decision:"reject"` only after the user rejects the result. The manifest returns to `skill-drafted`, and every Teaching artifact remains, along with the learning claim, so the same claim operation id can save an edited package. Run and pass the changed-input Dry Run again.
- **Verify and purge.** Call `agent_flow_skill_decide` with `decision:"verify"` only after the user chooses **Verify flow**. Cleanup first persists `purge-pending`, then deletes all raw Teaching files and the recording directory. Inspect `$CONTINGENCY_VERIFY_DIR/state/catalog`: the Flow Skill directory contains `SKILL.md`, its authored references, and `references/verification.md`; `.recordings/$recordingId/` is absent.
- **Recover cleanup after restart.** Stop the MCP process while the manifest is `purge-pending`, then start it again. Startup retries cleanup. If deletion still fails, the dock shows **Retry cleanup** and names the retained files; call `agent_flow_skill_decide` with `decision:"retry-cleanup"`. After success, the recording directory is absent. Restart MCP once more and start the verified Flow Skill from its persisted files to prove that cleanup did not remove it.
- **Release or report failure.** Before saving, `agent_teaching_recording_claim` with `action:"release"`, the claim id, and a fresh operation id returns the recording to `ready` and answers with `claim:null`. The same tool with `action:"fail"` records a non-empty `error` instead; the failed recording remains in `agent_teaching_recordings_list` and accepts a later claim. To prove crash recovery, stop the claiming MCP process without release, start a new one, and list again. The dead owner's recording returns to `ready`.
- **Inspect the public boundary.** Search every list, wait, claim, timeline, keyframe metadata, and save result for the disposable private literals, the absolute Catalog Root, `trace.zip`, and `recording.webm`. Require zero matches. Image bytes appear only in the single keyframe result.
- **Delete a recording.** From `Recording saved`, run `control-contingency browser click --role button --name "Delete recording"`, then wait for `Not recording`. The recording directory keeps `manifest.json` with `lifecycle` back at `setup` and no artifacts, and the video, Trace, and events files are gone.
- **Record again in the same browser.** Click `Start recording` from `Recording saved`, then Stop. The session keeps the same browser and receives a different `recordingId`; both recording directories remain distinct.
- **Proof (live Workspace).** Before closing Teaching, run `control-contingency browser goto --url "$viewUrl"`, `control-contingency browser wait --role heading --name "Teaching" --timeout-ms 20000`, then `control-contingency browser snapshot --aria --path workspace/teaching-live.aria.txt` and `control-contingency browser screenshot --path workspace/teaching-live.png`. The live Teaching region shows `Captured actions`, `Instructions`, the sensitive-artifact disclosure, user-owned browser controls, and no Takeover control. After the session closes it is no longer a live Workspace; prove the saved Flow Skill and the recording lifecycle through the MCP results and the isolated Catalog Root instead.
- **Proof (local artifacts).** After Stop, require non-empty `trace.zip`, `recording.webm`, and `events.jsonl` under `$CONTINGENCY_VERIFY_DIR/state/catalog/.recordings/$recordingId/`. The artifacts stay under isolated state and are not copied into proof artifacts; no absolute artifact path appears in any MCP result.
- **Proof (listed later).** Run `control-contingency mcp stop`, `control-contingency mcp start`, then `control-contingency mcp call --tool agent_flow_skills_list`. `agent_sessions_get` is empty in the new process, yet the verified Flow Skill is listed with its directory name, description, declared inputs, and step count.

### Finished Interactive Run

- **Finish a Run.** Use a verified Flow Skill from the Teaching steps above. Call `agent_flow_skill_run_start` with its `flowSkillName`, the fixture `url`, every declared input, client metadata, and a fresh `operationId`. Open the returned `viewUrl` and observe the Interactive Run in the Workspace. Drive the Steps with `agent_browser_snapshot` and `agent_run_step_assess`, citing a Browser Snapshot from the Step being assessed.
- **A Run ends itself.** Assessing the last ordered Step ends the Run without another call: the result reports `activeStepIndex: null`, `outcome: "completed"`, complete coverage, and an `endedAt`. `open_run` on its `runId` succeeds immediately. A `not-working`, `inconclusive`, or `blocked` verdict ends it the same way with `outcome: "ended-early"` and incomplete coverage.
- **End a Run early.** `agent_run_complete` with a `sessionId` and a fresh `operationId` ends a Run whose Steps are unfinished, which records incomplete coverage. On a Run that already ended it answers with the Summary that Run wrote and records the optional `agentAccount` on it; it never writes a second Summary.
- **Open the summary.** Call `open_run` with the returned `runId`. Open its `viewUrl` using `control-contingency browser goto --url "$viewUrl"`. The URL has path `/` and a `run` query, with no `session` query. Wait for `control-contingency browser wait --role heading --name "Run video"`. The Workspace navigation stays visible; Run Summary shows the outcome, coverage, assessments, and recorded video without a live browser canvas.
- **Reopen persisted evidence.** Restart the isolated MCP process, call `open_run` again, and open the new URL. Save `workspace/run-summary.aria.txt` and `workspace/run-summary.png`. Scroll to Run video and confirm the player is reachable. The summary survives the process restart.
- **Removed modes.** Open `/audit` and `/agent` and require `Page not found`. Return with the `Workspace` navigation link.

### Teaching private Variables

- **Start on the login fixture.** Start a Teaching session at `${ECOMMERCE_URL%/*}/login.html`, open its `viewUrl`, and click `Start recording`. The user holds the browser already, so there is nothing to take. Use `agent_browser_snapshot` first and note the refs for `Account ID`, `Password`, and `otp-input 1 of 6`.
- **Enter values as the user.** A private value is the user's to type, and there is no MCP tool for it: the agent can only ask. Drive the Workspace loopback `agent.teaching.variable.input` with the noted ref for `ACCOUNT_ID` (`secret`, not `runtime`), `PASSWORD` (`secret`, not `runtime`), and `OTP` (`secret` and `runtime`), each with a disposable literal and a fresh `operationId`. The OTP entry completes only after all six one-character controls accept the value, and its returned Snapshot includes `Verification ready`. The Workspace has no control for this yet, so drive the RPC directly until the unified Workspace ships one.
- **Proof (live Workspace).** Before closing Teaching, capture `workspace/private-variables.aria.txt` and `workspace/private-variables.png`. The proof shows the live user-led Teaching session with no control button, no `Enter private value` button, no private Variable dialog, and no private-value fields. Search the ARIA artifact for those removed controls and require zero matches.
- **Prove the timeline boundary.** After Stop, claim the recording and page through `agent_teaching_timeline_get`. The three captured fills contain `{{ACCOUNT_ID}}`, `{{PASSWORD}}`, and `{{OTP}}`. Search the complete stdout for all three disposable literals and require zero matches. Require the accessibility evidence to retain all six `otp-input N of 6` labels plus the unrelated `1800+` and `1940` copy. Cookies, authorization data, network bodies, Trace, raw video, and local artifact paths are absent.
- **Prove the Flow Skill declares rather than contains.** Save a `SKILL.md` over the three fills. Its frontmatter `inputs` names `ACCOUNT_ID`, `PASSWORD`, and `OTP` in SHOUTY_SNAKE_CASE, and its steps reference them rather than the literals. Read every saved file and require the disposable literals to be absent.
- **Proof (local sensitive artifacts).** After Stop, require non-empty `trace.zip` and `recording.webm` under `$CONTINGENCY_VERIFY_DIR/state/catalog/.recordings/$recordingId/`. Read `$sessionId.artifacts.json` in the same directory: it marks the named Trace and video with `"sensitive":true` and `"retention":"local"`. Do not copy the unredacted artifacts into `artifacts/`.

### Execution Boundary

- **Refuse an undemonstrated host.** Call `agent_flow_skill_run_start` for the verified Flow Skill with a `url` on a host the recording never visited, such as `localhost` in place of `127.0.0.1`. It exits `2` with `flow_skill_invalid` and names both the demonstrated hosts and the refused one. No Run session is created.
- **Start an Interactive Run.** Call `agent_flow_skill_run_start` with a verified Flow Skill from the Teaching recipe above, the fixture `url`, its declared inputs, client metadata, and a fresh operation id. The Run opens under the stamped Emulation rather than a default desktop window: read the session back and require its viewport to match the `emulation` block in `SKILL.md`. Open the returned `viewUrl`.
- **Assess each Step.** Drive the active Step, then call `agent_run_step_assess` with `outcome`, `explanation`, and at least one `evidence` entry naming a Snapshot id or attempt id that Step produced. A `working` verdict advances to the next ordered Step. `not-working`, `inconclusive`, or `blocked` ends the ordered Steps and leaves coverage incomplete.
- **Refuse a domain.** Call `agent_browser_act` with a navigate action whose URL replaces the fixture's `127.0.0.1` host with `localhost`. The result contains `intervention.reason: domain`; the browser stays on its previous page. Call `agent_session_get`: `pendingDecisions` holds one `boundary` decision whose `boundaryId` equals the intervention and whose `scopeSummary` names the exact host. Run `control-contingency browser wait --role heading --name "Waiting for confirmation"`. The region `Execution Boundary` shows the URL, action, operation id, and the pending id, and offers no allow or refuse button. Call `agent_pending_decision_resolve` with `decision:"refuse"` and that pending id after the user refuses in the MCP conversation. `boundary` is null and the timeline retains the refusal.
- **Take priority control.** Run `control-contingency browser click --role button --name "Take control"` while a Boundary is paused. Resolving that pending id with `decision:"allow"` exits `2` and reports that control must be returned first; `decision:"refuse"` still succeeds. Click `Return control` before allowing.
- **Confirm one attempt.** Navigate to the in-scope fixture host, observe the page, and request a click on a current element reference with `intent.irreversible` declared. It returns `intervention.reason: confirmation`. Resolve its pending id with `decision:"allow"`, then repeat the identical MCP operation id and action. It runs once. Replaying that id returns its original result; a new id requires another decision. Resolving the spent pending id again exits `2` as a conflict.
- **Phrase the active objective freely.** Supply `intent.objective` with a paraphrase of the active Step and omit `intent.objectiveKind`. The action proceeds without an objective intervention because actions belong to the active Step by default.
- **Request a new objective.** Supply `intent.objective` with an objective outside the Flow Skill's Steps and `intent.objectiveKind:"new"`. It returns `intervention.reason: objective`. Allowing that decision permits only that action attempt. A marked irreversible action still requires its own decision.
- **Capture proof.** Save ARIA and screenshots under `agent-boundary/` while paused, during Takeover, and after the allow. Re-read the session to prove the timeline, `decisionHistory`, and controller agree with the View. Dry Runs use the same relay.

### Runtime Variable decisions

- **Prepare a runtime Variable.** Follow the Teaching private Variables recipe and save a `SKILL.md` that declares at least one input in SHOUTY_SNAKE_CASE. Start its Dry Run with `agent_flow_skill_dry_run_start`; passing that input to the tool is refused.
- **Read each request.** The start result contains one `supply_variable` item in `pendingDecisions` for every missing runtime Variable. Each item names the Variable, states whether it is secret, and has its own `pendingDecisionId`. Open the Run's `viewUrl`. The `Runtime Variables` region shows the same names and ids, with no input or supply buttons.
- **Refuse without inventing coverage.** Call `agent_pending_decision_resolve` with `decision:"refuse"`, the Variable's pending id, and a fresh operation id. Call `agent_session_get` again. The pending item is gone, the Variable remains `supplied:false`, `activeStepIndex` is unchanged, and `decisionHistory` names the Variable without a value. Assess the blocked Step and report the Dry Run as failed to record incomplete coverage.
- **Retry and supply.** Start another Dry Run with a fresh operation id. Resolve its Variable decision with `decision:"supply"` and `value:"<disposable literal>"`. Repeat the exact call with the same operation id. Both results are identical, the pending item is gone, and the Variable reads `supplied:true` without exposing the literal.
- **Use the supplied Variable.** Call `agent_browser_snapshot`, then `agent_variable_enter` with the Variable name and destination ref. The action succeeds and returns a redacted Snapshot. Assess the Step as `working`; the ordered Run can proceed.
- **Capture proof.** Save `workspace/runtime-variable-pending.aria.txt` and `.png` before resolving, then `workspace/runtime-variable-refused.*` and `workspace/runtime-variable-supplied.*`. Search every ARIA artifact and MCP result for the disposable literals and require zero matches. Cleanup must remove the isolated catalog, Trace, and video while these proof files remain.

### Browser setup

- **Open the panel.** With a live Teaching session open at its `viewUrl`, run `control-contingency browser click --role button --name "Browser setup"`. The `Browser setup` region appears under the canvas with `User agent`, `Device`, and `Emulation` controls and the Console, Network, and Storage tabs.
- **Change the identity.** Run `control-contingency browser click --role combobox --name "User agent"` and choose `Chrome — Android Mobile`. The identity brings its own device metrics: the applied viewport beside the controls changes with it, and the live canvas re-renders at phone width.
- **Change the environment.** Open `Emulation`, fill `Locale` with `de-DE` and `Time zone` with `Europe/Berlin`, and choose a colour scheme. Read the session's browser back with `agent_browser_snapshot`; the page observes the values it was given.
- **Inspect storage.** Choose the `Storage` tab, then the `Cookies`, `localStorage`, and `sessionStorage` inner tabs. Entries belong to the tab the session is showing. Add and delete an entry and confirm `Clear this store?` before clearing.
- **Refuse during agent control.** On an Interactive Run, open the same panel while the agent holds the browser. The controls are disabled and the panel explains that the agent holds the browser. Taking control enables storage inspection but Emulation still refuses: a Run reproduces the Emulation the Flow Skill was demonstrated under.
- **Proof.** Save `workspace/browser-setup.aria.txt` and `workspace/browser-setup.png` with the applied identity and viewport visible, plus `workspace/browser-setup-storage.png` over the Storage tab.

## Gotchas

- `Loading Agent Sessions…` is transient. Wait for the empty heading or the requested session result. Do not snapshot the spinner.
- No live Workspace has a header band. Do not wait for `link Workspace` or a `Workspace` heading on `/` with no session or on any live session; wait for the `Workspace dock` region instead.
- The action timeline is no longer rendered anywhere in Workspace. Assert it through `agent_session_get`.
- Inspect hit-tests the live Page through a Browser Snapshot. A stale outline means the Page moved, not that the click missed; re-hover rather than clicking the canvas as if it were the DOM.
- Empty means the current `web` or `mcp` process owns zero sessions.
- A bad `?session=` value names a session that the current process does not own. With no replacement, the notice names the server process. With a live replacement, the URL adopts its id and the dock stays available.
- Closing Workspace does not pause a real session.
- Every featured product in the shop fixture has an `Add <product> to cart` button. Clicking a product heading or its SKU paragraph is inert, and an inert click recorded as a successful action is a demonstration a learning agent cannot read (#215). Target the button.
- Do not start `mcp` on 7777 if the user already has Contingency there. `mcp start` picks its own port.
- Only the user returns control. There is no MCP tool for it by design; an agent may only ask with `agent_session_takeover_request`. A drive that needs the agent driving again must click `Return control`.
- Consecutive ordinary text edits during Takeover are coalesced into one semantic `fill`. Other low-level user input remains a redacted mouse or keyboard event.
- `agent_browser_screenshot` and `agent_browser_snapshot` keep working while the user holds control. Observation is not gated on control; action is.
- `agent_browser_screenshot` answers with `path`, `bytes`, `format`, `capturedAt`, and `url` — never the image bytes (#240). Read the PNG at `path` yourself; it is deleted when the Agent Session closes.
- A failed tool call exits non-zero and prints the reason (`Element reference e12 is stale…`, `Could not find "X": Timeout…`). Assert on that text rather than on the exit code alone.
- Element references expire when the Page navigates. Take a fresh `agent_browser_snapshot` after any navigation before acting on a `ref`.
- Only `"activity":"teaching"` sessions capture. `agent_teaching_instruction_record` against a Run session exits `2` with `agent_session_invalid`.
- Flow Skill learning uses the process-independent Teaching Recording tools after Stop. It needs neither `agent_session_close` nor the live Agent Session.
- Domain Scope comes from the `hosts` the Flow Skill's frontmatter records, which is every host the Teaching Recording visited. A Run that opens elsewhere is refused before a session exists; only a package saved before stamping falls back to the opened host.
- An instruction joins the recording's event stream in order, so a learning agent reads it beside the actions it explains.
- Browser setup writes need the user to hold the browser, and Emulation changes need a Teaching session. An Interactive Run reproduces the Emulation the Flow Skill was demonstrated under, so `agent.browser.emulation.set` is refused there even during Takeover.
- The setup panel names the Agent Session, never a browser session id. There is no generic `browser.*` route into an Agent Session's browser.
- A saved Flow Skill is not verified coverage. `agent_flow_skill_decide` with `decision:"verify"` is the only call that purges the Teaching Recording, and only the user's **Verify flow** choice authorizes it.
